# Release Notes v2.5.11

**Release Date:** 2025-11-10  
**Type:** UI Improvements

## 🎨 UI Improvements

### Progressbar UX Verbesserungen

**Problem:**
- `header-status-basic` zeigte "Idle" an, während die Progressbar auch "Idle (mm:ss)" anzeigte
- Redundante Status-Anzeige war verwirrend und unübersichtlich

**Solution:**
- `header-status-basic` wird automatisch ausgeblendet, wenn die Progressbar angezeigt wird
- Wenn die Progressbar nicht angezeigt wird (Processing, Paused, Manual Mode), wird `header-status-basic` wieder angezeigt
- Saubereres, konsistentes UI-Design ohne redundante Informationen

**Impact:**
- Keine redundante "Idle" Anzeige mehr
- Klareres, übersichtlicheres UI
- Bessere UX durch konsistente Status-Anzeige

### Footer Cleanup

**Problem:**
- Footer zeigte "Auto-refresh every 2 seconds" an
- Text war nicht mehr korrekt, da Smart Refresh implementiert wurde (nur bei Änderungen)
- Verwirrende Information für Benutzer

**Solution:**
- "Auto-refresh every 2 seconds" Text komplett entfernt
- Footer zeigt jetzt nur noch relevante Informationen (Last update, Version, GitHub Link)
- JavaScript-Kommentare aktualisiert für Smart Refresh

**Impact:**
- Saubererer Footer ohne verwirrende Informationen
- Klarere Kommunikation der Funktionalität

## 🔧 Technical Details

### Code Changes

**`src/cineripr/web/templates/index.html`:**
- Footer: "Auto-refresh every 2 seconds" entfernt

**`src/cineripr/web/static/js/app.js`:**
- `header-status-basic` wird dynamisch ein-/ausgeblendet basierend auf Progressbar-Sichtbarkeit
- Kommentare aktualisiert: "Smart refresh: Updates only when data changes"

## 📦 Migration

No migration required. This is a UI improvement that works with existing configurations.

## 🚀 Upgrade Instructions

1. Pull the latest Docker image: `docker pull ghcr.io/rokk001/cineripr:latest`
2. Restart your container
3. UI sollte jetzt sauberer und konsistenter sein

---

**Full Changelog:** [v2.5.10...v2.5.11](https://github.com/Rokk001/CineRipR/compare/v2.5.10...v2.5.11)

